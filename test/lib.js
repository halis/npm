'use strict';

const expect = require( 'chai' ).expect;
const util = require( '../src/lib.js' );

describe( '', () => {
	let ;

	describe( '#()', () => {

		it( '', () => {
			expect( ).to.equal( );
			expect( ).to.equal( );
			expect( ).to.equal( );
		});

		it( '', () => {
			expect( ).to.equal( );
			expect( ).to.equal( );
			expect( ).to.equal( );
		});

		it( '', () => {
			expect( ).to.equal( );
			expect( ).to.equal( );
			expect( ).to.equal( );
		});

	});

	describe( '#()', () => {

		it( '', () => {
			expect( ).to.equal( );
			expect( ).to.equal( );
			expect( ).to.equal( );
		});

		it( '', () => {
			expect( ).to.equal( );
			expect( ).to.equal( );
			expect( ).to.equal( );
		});

		it( '', () => {
			expect( ).to.equal( );
			expect( ).to.equal( );
			expect( ).to.equal( );
		});

	});

});
